# web

UI + console (later phase). Adapts heavener's console stack (ClickHouse / Postgres / Redis /
API / React) for live alerts, an investigation view, and a coverage atlas. See
`docs/ROADMAP.md` Phase 3.
